<header class="header">
    <a id="name_site" href="/">MovieLib</a>
    <div>
        <div id="movie_search">
            <input />
            <button>Поиск</button>
        </div>
        @if (ViewBag.Button != null)
        {
            <a asp-action=@ViewBag.Button asp-route-id="@ViewBag.ID" id="header_button">
                @ViewBag.ButtonText
            </a>
        }
    </div>
</header>